# M1 Receipts — 2026-04-19

本文档记录本轮 M1 closeout 的实际运行证据。

## 1. Receipt run 基本信息

- run id: `m1-closeout-20260419-163831`
- isolated runtime home: `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home`
- build version: `0.1.0001`
- standardized receipt index:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/receipt-index.json`
- install runtime receipt index:
  - `~/.fin/harness/reports/0.1.0001/receipt-index.json`

## 2. build/install gate 现状

本轮尝试走正式：

```bash
cargo run -p fin-cli --manifest-path rust/Cargo.toml -- install-dev ~/.fin/config/user.toml 0.1.0001
```

### 2.1 初始 closeout run 结果

- **被 line-limit gate 阻断**
- 证据：`~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/logs/install/0.1.0001.log`

当前阻断文件：

- `rust/crates/cli/src/session_commands.rs` — 603
- `rust/crates/cli/src/tests.rs` — 679
- `rust/crates/cli/src/web_debug.rs` — 1314
- `rust/crates/debug-server/src/tests.rs` — 577
- `rust/crates/runtime/src/lib.rs` — 1153
- `rust/crates/runtime/src/model_output_tests.rs` — 670
- `rust/crates/runtime/src/session_materializer.rs` — 698

结论：

- 正式 build/install 自动门禁仍未收口
- 但不阻塞本轮使用 **隔离 manual receipts** 完成 M1 功能证据

### 2.2 gate recovery 后复跑结果

在后续收口修复中，已完成：

1. long-file 拆分回到 `< 500` 行门禁内
2. `cargo fmt --all --check`
3. `cargo test -p fin-runtime -p fin-cli -p fin-debug-server --manifest-path rust/Cargo.toml`
4. 再次执行正式 `install-dev`

实际结果：

- **已通过**
- install log:
  - `~/.fin/logs/install/0.1.0001.log`
- regression log:
  - `~/.fin/logs/regression/0.1.0001.log`
- harness summary:
  - `~/.fin/harness/reports/0.1.0001/summary.json`

验证到的事实：

- `~/.fin/install/versions/0.1.0001` 已生成
- `~/.fin/bin/fin` 已指向 `~/.fin/install/current/bin/fin`
- install flow 内部已完成：
  1. `config-check`
  2. `runtime-demo`
  3. `debug-projection`
  4. post-install `config-check`

结论更新：

- 正式 build/install gate **已恢复**
- M1 closeout 的 build/install 阻断已清除

## 3. installed-binary smoke receipt

### 3.1 结果

- **通过**
- receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/installed-binary-smoke-manual.json`
- regression log:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/logs/regression/0.1.0001-manual.log`

### 3.2 实际验证内容

使用已编译 binary 手工搭建隔离 install layout，并完成：

1. staged binary `config-check`
2. staged binary `runtime-demo`
3. staged binary `debug-projection`
4. current bin link `config-check`

### 3.3 验证到的事实

- installed binary 可运行
- 能读取隔离 `user.toml`
- 能写入：
  - `runtime/current/last_run.json`
  - `runtime/current/current_context.json`
  - `runtime/projections/current_snapshot.json`
  - `runtime/projections/current_projection.json`
  - session `context/recent_contexts.json`
  - session `conversation/messages.json`

## 4. provider real smoke receipt

### 4.1 结果

- **通过**
- receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/provider-probe.json`

### 4.2 关键事实

- provider: `ali-coding-plan`
- protocol: `anthropic-wire`
- model: `qwen3.6-plus`
- endpoint: `https://coding.dashscope.aliyuncs.com/apps/anthropic/v1/messages`
- user-agent: `opencode/1.2.27`
- http status: `200`
- output_text: `OK`

结论：

- 默认 anthropic provider 真实可用

## 5. compact rebuild receipt

### 5.1 结果

- **通过**
- transcript seed:
  - `fixtures/transcripts/banana-memory.json`
- web-debug compact receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/web-debug-compact-response.json`
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/web-debug-handcheck.json`

### 5.2 验证到的事实

先用 transcript-demo 生成固定 3 turn 历史，再通过 4040 live `/compact` 验证：

- `/compact` 返回 `response_kind=system_notice`
- `events_count=0`
- `runtime/current/current_context.json` hash 改变
- `runtime/current/current_rebuild_index.json` hash 改变
- `session context/recent_contexts.json` 计数从 `3 -> 4`
- `conversation/messages.json` hash 改变（因为框架追加 `/compact` notice）

结论：

- `/compact` 走的是 framework rebuild
- 没有走 provider turn

## 6. 4040 web debug truth receipt

### 6.1 结果

- **通过**
- binding receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/web-debug-binding.json`
- status receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/web-debug-status-response.json`
- summary receipt:
  - `~/.fin/harness/runs/m1-closeout-20260419-163831/runtime-home/harness/reports/0.1.0001/web-debug-handcheck.json`

### 6.2 关键事实

4040 live binding 指向：

- session: `session-test-real-transcript`
- task: `task-test-real-transcript`

`/status` hand-check：

- `response_kind = status_probe`
- `events_count = 0`
- `freshness = recent`
- `messages.json` hash 不变
- `recent_digests.json` hash 不变
- `recent_contexts.json` count 不变

结论：

- `/status` 没有写入新 closure / digest
- Web debug 当前读取的是 session truth，而不是自造状态

## 7. 当前收口判断

本轮 receipts 已补齐：

1. installed-binary smoke — 已有 manual receipt
2. provider real smoke — 已有 real receipt
3. compact rebuild — 已有 live receipt
4. 4040 web debug truth — 已有 live receipt

当前剩余 closeout 缺口主要变成：

1. installed-binary smoke 的 receipt 体系仍需继续标准化
2. closeout 文档与 receipts 需要随着后续收口持续同步，不得再漂移
