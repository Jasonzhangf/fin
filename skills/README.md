# fin local skills

本目录只存放项目本地执行 skills。

- 跨项目通用规则放全局 skills（例如 `coding-principals`）。
- `fin` 项目特有的架构边界、状态机、路由、验证门禁与反模式，统一沉淀到这里的本地 skills。

- `fin-general-dev`：通用开发闭环
- `fin-architecture`：分层、crate、唯一真源
- `fin-testing-harness`：测试、回放、故障注入
- `fin-runtime-debug`：事件定位与 runtime 调试
